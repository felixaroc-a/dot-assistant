/**
 * Tipos generados desde OpenAPI del backend DOT.
 * Regenerar con el API en marcha: npm run api:types
 * (requiere devDependency openapi-typescript instalada localmente)
 */
export type paths = Record<string, never>
