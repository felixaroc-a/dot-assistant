/**
 * Workspace principal (cabecera, automatizaciones, chat).
 * Estructura alineada a un backend por módulos: model/ + components/.
 */
export { DashboardShell } from './DashboardShell'
export type { DashboardShellProps } from './DashboardShell'
export type { AgentId, SavedAutomation, ChatMessage, ChatRole } from './model/types'
