/**
 * Punto de exportación histórico empleado desde la carpeta de onboarding.
 * Reexportamos la implementación común para evitar romper los import existentes.
 */
export { isPendingVerificationStatus } from '@/lib/whatsapp/status'
